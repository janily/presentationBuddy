diff --git a/README.md b/README.md
index 8d163c9..d1e4fe4 100644
--- a/README.md
+++ b/README.md
@@ -13,3 +13,3 @@ Install dependencies first:
 ```bash
-pnpm install
+npm ci
 ```
@@ -19,3 +19,3 @@ Run the Next.js web application:
 ```bash
-pnpm dev
+npm run dev
 ```
@@ -27,3 +27,3 @@ If you want to run or inspect Mastra workflows directly, start the Mastra develo
 ```bash
-pnpm mastra
+npm run mastra
 ```
@@ -106 +106,19 @@ For Zeabur, mount a persistent volume such as `/data`, then set `GENERATED_SLIDE
 For Vercel, the app writes generated decks to `/tmp/generated-slides` and exports `maxDuration = 300` on the analyze route. This is only suitable for short-lived previews; files in `/tmp` are not durable and may disappear after function restarts. For durable production history on serverless platforms, use object storage such as S3, R2, or GCS.
+
+
+## Quality checks and deployment boundaries
+
+Use Node.js 22+ and npm 10+ with the committed `package-lock.json`. `npm ci` is the canonical installation command; do not maintain a second, divergent pnpm lockfile.
+
+```bash
+npm ci
+npm run check
+```
+
+Generated HTML runs in an opaque-origin sandbox with inline navigation enabled. The application download button exports the original HTML. Embedded previews and direct `/api/preview/*` and `/generated-slides/*` responses restrict network APIs, external scripts, forms and origin access. Remote HTTPS fonts/images remain supported.
+
+`npm run check:runtime-assets` verifies that the production deployment trace includes the actual skill and template files needed at runtime. New template directories must continue to pass this check.
+
+**Deployment limitations:** workflow run storage uses in-memory LibSQL, artifact/proposal stores are process-local, and Vercel preview files use temporary disk. These are not durable multi-instance storage or multi-user access controls. A public production service needs authenticated ownership checks, rate/cost limits, shared durable workflow/proposal/artifact storage and a request/job timeout strategy before launch. This audit does not add those infrastructure services or claim that local success proves serverless multi-instance reliability.
+
+See `docs/audits/2026-09-24-report.md` for verified repairs, evidence and remaining work.
diff --git a/docs/audits/2026-09-24-plan.md b/docs/audits/2026-09-24-plan.md
new file mode 100644
index 0000000..99728ca
--- /dev/null
+++ b/docs/audits/2026-09-24-plan.md
@@ -0,0 +1,28 @@
+# Reliability, experience and performance audit
+
+## Goal
+Preserve the existing brief → outline review → generated HTML product while making its boundaries trustworthy. Success means accepted user choices survive the API boundary, generated code cannot control the studio, cancelled work cannot publish late updates, and deterministic regression tests plus production compilation pass.
+
+## Scope and decisions
+- Fix reproducible defects in the existing Next.js/Mastra architecture; do not rewrite the product or replace model providers.
+- Work only on `fix/reliability-performance-audit-20260924`; never merge or deploy production in this audit.
+- Compare the synchronized npm lockfile with the stale pnpm lock before selecting one canonical install path. Do not silently install unpinned dependency trees.
+- No real model API keys, paid generation or production customer content are used in validation.
+- Treat source validation as a structural heuristic, not a guarantee of rendered design quality.
+
+## Execution sequence
+1. Reproduce clean installation, unit tests, lint, type checking and production build. Record baseline failures separately.
+2. Write failing route tests for lost generation options and malformed JSON; preserve the validated input and return a client error for malformed input.
+3. Write failing preview tests: opaque iframe origin, standalone/static response sandbox, input validation and correct file error statuses. Preserve inline navigation and parent-controlled HTML download.
+4. Write failing skill-loader tests for traversal and concurrent repeated reads; resolve only trusted bundled paths, cache successful immutable reads and evict failed reads.
+5. Write failing cancellation/resource tests. Ignore events after cancellation and clear timeout handles after each completed stream operation.
+6. Remove repeated whole-history flattening; test newest run precedence and suspended outline lookup independently of object insertion order.
+7. Preserve the preview browsing context while a revision starts/stops and improve the small-screen workspace. Verify in a browser where execution is available.
+8. Re-run the complete suite, lint, types and production build. Perform a separate author self-review, record outstanding risks, and publish branch changes plus an auditable report.
+
+## Review focus
+- New start vs resume vs revision must keep existing wire contracts.
+- Missing/aborted streams must not leak readers, timers, or uncaught cancellation promises.
+- Direct generated-file URLs must be isolated as well as embedded previews.
+- Unsafe style metadata must not read files outside the bundled skill.
+- In-memory workflow/artifact/proposal state is not durable or distributed consistency; this remains an explicit deployment limitation unless independently addressed and tested.
diff --git a/docs/audits/2026-09-24-report.md b/docs/audits/2026-09-24-report.md
new file mode 100644
index 0000000..9884d74
--- /dev/null
+++ b/docs/audits/2026-09-24-report.md
@@ -0,0 +1,77 @@
+# Presentation Buddy：代码、体验与性能审计
+
+日期：2026-09-24。审计分支：`fix/reliability-performance-audit-20260924`。
+
+## 结论与边界
+
+本项目的核心价值是把用户确认的内容和风格，可靠地变成可预览、可下载的演示文稿，而不是叠加更多 Agent、状态层或提示词。此次保留 Next.js + Mastra 及既有使用流程，优先修复边界丢失、取消失效、预览信任边界和部署缺文件等可复现问题。
+
+这是一轮有测试证据的修复，不代表全项目已经无缺陷或具备公开多用户生产服务的条件。没有使用真实模型密钥，没有调用付费模型，也没有执行真实客户内容生成。未修改 main，未合并或部署生产。当前环境的本地浏览器访问被管理策略拦截，因此不把源码/SSR 检查等同于浏览器视觉验收。
+
+## 已修复的问题
+
+| 优先级 | 问题与根因 | 修复 | 证据 |
+| --- | --- | --- | --- |
+| 高 | API 已验证 `styleSpec`、`density`、`purpose` 等字段，却在启动工作流时重新手工选字段，丢失用户选择 | 直接传入已验证的完整数据，保留 start/resume/revise 既有协议 | `src/app/api/analyze/route.test.ts`，包含完整输入、Agent 上下文与 resume 回归 |
+| 高 | 非法 JSON 被当成服务端未知错误返回 500 | 在 JSON 解析边界返回 400 / `invalid_json`，不启动工作流 | 同一 API 测试文件 |
+| 高 | 预览 iframe 同时启用脚本和同源权限，生成 HTML 的直接访问也没有响应级隔离 | iframe 只保留 `allow-scripts`；API 和静态生成文件路径统一 sandbox CSP、禁止网络 API/外部脚本/表单，禁发 referrer；原始 HTML 仍由父页面下载 | 预览组件 SSR 测试、API 响应测试和 Next headers 配置；浏览器隔离效果尚需真实浏览器验证 |
+| 高 | 将任意文件读取错误都报告为 404，且自托管预览被公开缓存一年 | 文件名白名单；ENOENT 才返回 404，其他读取故障返回 500；生成内容采用 private/no-store | `src/app/api/preview/[filename]/route.test.ts` |
+| 高 | 模板路径来自请求元数据，可在拼接路径时越出技能目录 | 仅允许内置文件或严格模板 slug + 固定相对路径；非法路径在文件读取前拒绝 | `skill-loader.test.ts` 中的路径穿越、绝对路径和反斜杠用例 |
+| 高 | 打包器无法追踪运行时动态拼接的技能和模板文件；编译成功不等于部署后可读文件 | 显式 `outputFileTracingIncludes`；新增构建后真实 trace 清单检查 | 修复前检查缺少 39 个 analyze 必需资源；修复后 analyze 39 个、style-discovery 2 个全部进入 trace |
+| 中 | 同一批并发生成重复读取相同技能文件 | 按可信完整路径缓存读取 Promise，合并并发读；失败缓存删除，开发模式不缓存 | 两次并发上下文加载的读取次数从 12 次变为 6 次；失败重试与开发路径回归测试 |
+| 中 | 每个流式片段创建 Promise.race 的失败定时器，成功后定时器仍等待到期 | 统一 `withTimeout`，成功、失败、超时、取消均清理定时器及监听器；HTML read 等待不超过本轮剩余预算 | `with-timeout.test.ts` 的假时钟测试；不把它表述为上游模型计费取消保证 |
+| 中 | 已取消的会话仍能接收晚到内容/决定；读取器在异常路径没有可靠释放 | 取消后忽略所有回调；抽取流消费器，取消未结束流并释放 reader lock，不等待不合作的 cancel Promise | 取消、错误、无最终结果、晚到 chunk、永不结束的取消回调用例 |
+| 中 | 多次 flatMap/filter 扫描全部历史；旧任务的 HTML 和状态可能混入新任务 | memo 化 transport 和单次反向扫描，以服务端 runId 划分边界；只使用 assistant 数据；按指定挂起步骤取大纲 | `use-presentation-workflow.test.ts` 的新旧任务隔离、步骤顺序及伪造用户 ID 用例 |
+| 中 | 从预览进入修订时 React 树位置变化，iframe 被重新挂载 | 保持同一父节点与 HtmlPreview 位置；修订状态用非替换式提示覆盖 | 组件结构回归测试；未声称浏览器当前页/焦点已实测 |
+| 中 | 用字符串包含 `.mastra` 识别运行目录，误伤 `my.mastra-app` 等合法项目名；保存前缀可携带路径 | 按真实目录段识别 `.mastra`；文件前缀白名单 | 技能加载与保存测试；修复测试中恢复环境变量为字符串 `undefined` 的污染 |
+| 中 | 小屏幕双面板依靠隐式 grid 行高，长内容可能挤出聊天区 | 使用动态视口高度和明确 minmax 行分配，添加区域可访问性名称；桌面双列保持不变 | `presentation-workspace.test.ts` 的 SSR 结构检查；实际手机体验仍需验收 |
+| 中 | pnpm 锁文件缺少两个依赖，npm 锁文件大量绑定腾讯镜像 | 选择已同步的 npm 锁文件；只替换镜像下载地址，保留版本和 integrity；移除过期 pnpm 锁与 workspace 文件 | 原始 pnpm frozen install 实际失败；规范化后 npm ci、原有测试和构建成功 |
+
+## 验证记录
+
+### 基线
+
+- 原始严格 pnpm 安装失败：`ERR_PNPM_OUTDATED_LOCKFILE`，缺 `react-markdown` 和 `remark-gfm`。
+- 规范化 npm 下载源后，GitHub Actions run `36016859332` 的安装、测试、Lint、类型检查、生产构建均成功。
+- 本地对原始业务源码复跑：29 个测试文件，171 项通过。
+
+### 修复后
+
+- 37 个测试文件，211 项测试通过，新增 40 项。
+- `tsc --noEmit` 通过。
+- `next build` 通过。
+- 构建产物清单检查：analyze 39 个必需文件、style-discovery 2 个必需文件全部存在。
+- Lint 无错误，但保留 3 条原有 unused-variable/import warning；没有关闭规则掩盖问题。
+- 新增回归测试在修复前出现对应失败；不能把所有测试都理解为端到端模型或浏览器测试。
+- 最终远端提交的 CI 结果，以该分支 GitHub Actions 页面为准；不要把基线 CI 的成功误认作最终提交验证。
+
+这些证据证明输入/状态/资源处理及构建边界的确定性行为，不证明真实生成速度、模型内容正确性、视觉一致性、手机交互质量或多实例可靠性。未提供无依据的“性能提升百分比”。
+
+## 仍需优先处理的生产风险
+
+### P0：公开服务之前必须处理
+
+1. **工作流持久化与多实例一致性**：`src/mastra/index.ts` 使用内存 LibSQL，artifact/proposal store 也是进程内 Map。冷启动、扩容、不同函数实例间不能保证 resume、proposal 和版本检查可用。需要共享持久存储、原子版本更新及幂等操作，并通过跨实例、重启恢复和并发修订测试。不能只挂载 HTML 目录就声称整个工作流已经持久化。
+2. **鉴权、所有权与成本控制**：缺少面向多用户的完整资源归属、访问权限、请求限流、生成额度与并发控制。CSP 隔离不是认证机制，随机文件名也不是授权。公开运行前应完成这套边界及相应滥用测试。
+3. **请求生命周期与模型执行预算**：API `maxDuration=300`，原工作流多个生产超时预算大于该上限。此次清理定时器和约束单次 read 等待，不等于解决无服务器长任务。需要选择可恢复后台任务，或把全部尝试/修复/保存纳入统一请求截止时间，并验证真正传递到上游的取消信号。已有 abort listener 的注册时机与 `run.cancel()` 失败处理也需专门测试。
+
+### P1：下一轮质量与体验
+
+- **统一 AI SDK 大版本**：直接依赖的 `ai` 与 `@ai-sdk/react` 内部版本存在跨代关系，代码用 `as never` 隐藏部分 transport 类型不匹配。本轮不盲目升级依赖；应统一版本并在真实 SSE、工具事件、abort、resume 下验证。
+- **真实渲染验收**：已有风格检查主要是 HTML/CSS 结构与字符串规则，不是浏览器 computed style 或截图比较。需要用受控 deck 测试页数、溢出、字体加载、图表、键盘导航、手机布局与 iframe 隔离，并补真实模型样本。不能把检测到 class/变量当作视觉合格。
+- **错误恢复闭环**：验证中途断网、浏览器刷新、模型限流、文件保存失败和过期 proposal；区分可恢复步骤与必须重开任务，不只增加重试次数。
+- **流式客户端性能测量**：本轮消除重复数组扫描和技能重复读取，但没有在线 Core Web Vitals、长会话内存或真实生成 P95 数据。下一步以生产采样和固定脚本比较，而非猜测优化收益。
+- **文案与维护性**：风格选择中“按当前主题生成标题页”的文案与静态预览来源需要统一；大组件和 workflow 可在行为测试完善后按职责小步拆分。不要为了文件更短重写整条流程。
+
+## 复核与使用
+
+本轮最终代码检查是作者自复核，没有独立第二位 reviewer；合并前建议由项目维护者重点复核 CSP 兼容性、完整请求字段的语义、新旧任务边界以及生产部署限制。
+
+```bash
+git fetch origin
+git switch fix/reliability-performance-audit-20260924
+npm ci
+npm run check
+```
+
+人工验收：选择一套非默认风格和密度，确认大纲/最终生成继承它们；取消后立刻开启另一任务，观察旧数据是否回流；在预览中切到后续页再发起修订；测试 390px 手机视口；打开直接生成文件 URL 检查隔离及下载；再用真实模型做 start/resume/revise 全流程。任何一项未验证，不应在发布说明中写作“已通过”。
